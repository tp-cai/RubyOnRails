%h1= @doc.title
%p= @doc.content

= link_to "ALl Docs", docs_path
= link_to "Edit", edit_doc_path(@doc)
= link_to "Delete", doc_path(@doc), method: :delete, data: { confirm: "Are you sure to delete this file?"}
